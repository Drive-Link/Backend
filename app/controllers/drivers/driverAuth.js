const CreateDriver = async (request, response) => {
  // TODO - create drivers
}

const LoginDriverIn = async (request, response) => {
  // TODO - login drivers
}

module.exports = { CreateDriver, LoginDriverIn }
